# SPDX-License-Identifier: Apache-2.0
# Pinned toolchain — one compiler version for CI, contributors and the proofs alike.

[toolchain]
channel = "1.97"
components = ["rustfmt", "clippy"]
targets = [
    "x86_64-unknown-linux-musl",
    "aarch64-unknown-linux-musl",
]
profile = "minimal"
