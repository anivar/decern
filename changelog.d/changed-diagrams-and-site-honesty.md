- **Diagrams and site claims match the four-posture server.** The decision-flow and
  architecture diagrams name all four caller postures, workload binding / `caller_mismatch`,
  proofs over the model, and that `--anchor` (not the chain alone) detects a dropped tail.
  Editable SVG sources ship beside the PNGs. The site hero and Limitations link no longer
  overclaim what the chain proves or point only at the short README list. Authored by @anivar.
