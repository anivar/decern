---
name: Bug report
about: Something behaves incorrectly
labels: bug
---

**What happened**

**Expected**

**Repro** — exact commands, the model/entities, and the request:

```
```

**Affected crate / area** (if known — see [ARCHITECTURE.md](../../ARCHITECTURE.md))

**Environment** — OS, `rustc --version`, cvc5 version (if a proof is involved), `--features postgres`?

<!-- Human or agent, thanks for the report. If you already have a fix, a PR that closes this is welcome
     (`./scripts/verify.sh` must pass; DCO sign-off). See CONTRIBUTING.md. -->
